# Pruebas — COOP-R14

Cuarenta y siete suites. La mayoría corre contra el servidor de verdad: levantan un proceso,
abren WebSockets, mandan posiciones GPS y leen la base. **No hay mocks.** Es a
propósito: casi todo lo que se rompió en este proyecto se rompió en la juntura
entre el servidor, la base y el tiempo real, y un mock de cualquiera de los
tres habría tapado justo eso.

## Correr todo

```bash
cd pruebas && npm install     # solo la primera vez
npm test
```

Sale una línea por suite y un veredicto:

```
tramos     ok
objetivo   ok
...
=== REGRESIÓN VERDE ===
```

Una suite en rojo imprime sus fallas debajo. El código de salida es 1 si algo
falló, así que sirve en CI tal cual.

## Correr una sola

Ocho suites **esperan un servidor ya levantado en 3001** con la base a la que
ellas van a mirar. Las demás levantan el suyo solas, y trece no necesitan
ninguno: prueban lógica pura —de la app nativa, del trazador del creador y
las versiones—.

```bash
# las que necesitan servidor: tramos objetivo informes desvio turnos privado seguridad empresas
DB=/tmp/una.db
# MODO=demo no es opcional acá: sin él el arranque cuenta como producción y
# rechaza `despacho99`, que está en este repositorio y por eso ya no es secreta.
PORT=3001 DB_FILE=$DB DISPATCH_PASSWORD=despacho99 MODO=demo node ../server/index.js &
DBFILE=$DB DB_FILE=$DB node turnos.js

# las que se arman solas: variantes brecha creador gerencia cliente senal gpshttp presencia foto marca respaldo
# las que se arman solas (cont.): vendor retencion renovacion cascada mapa-shot compresion sos perfil grabador cobradores metidos puertas
# las que no necesitan servidor: trazador ausencia hud chat cola margenes gestos imagen tema contraste mapa teclado nativas envio pedido parada csv reloj paneles
# las que se arman solas (cont.): cadena huecos
# y trafico levanta el suyo, como gpshttp
node gerencia.js
```

Tres detalles que cuestan una tarde si no están escritos:

- **Los servidores de las pruebas llevan `MODO=demo`.** No es cosmético: el
  criterio del arranque es que **producción es todo lo que no se declare
  demo**, y en producción se revisan las llaves — no arranca sin
  `DISPATCH_PASSWORD`, ni con una quemada, y `despacho99` está quemada porque
  está en este repositorio, que es público. Un servidor de prueba ES una
  instancia descartable, así que declararlo es lo correcto y no un rodeo.
  El efecto secundario a tener presente: **las pruebas no pasan por los
  guardias de producción**. El único lugar que los ejerce es `puertas`, que
  levanta servidores en modo producción a propósito; cualquier guardia nuevo
  que se cuelgue de "no es demo" hay que probarlo ahí o queda sin probar.

- **Se pasan `DBFILE` y `DB_FILE`**, las dos. Unas suites leen una y otras la
  otra; unificarlas es un cambio de una línea que nadie hizo todavía.
- **Las suites no son re-entrantes entre sí.** Cada una asume una base recién
  creada: `turnos` cuenta los turnos que hay y `objetivo` cuenta las vueltas.
  Corridas todas contra la misma base dan rojo por motivos que no tienen nada
  que ver con el código. Por eso `regresion.js` le da a cada una su propio
  servidor y su propio archivo, y **espera a que el puerto 3001 quede libre**
  entre una y otra: sin esa espera el servidor nuevo no puede atarse al puerto,
  se muere, y la suite le habla al de la suite anterior.

## Qué cubre cada una

| Suite | Qué defiende |
| --- | --- |
| `tramos` | El circuito es ida + vuelta: progreso, tramo en el que va cada unidad y brechas sobre el circuito entero. Desde el 8/9, una de ida y otra de vuelta **no tienen brecha entre ellas**: la cadena es por tramo |
| `objetivo` | El objetivo de brecha automático: cuándo confía en el historial, cuándo cae al manual, y que el día de la semana no se mezcle. Cerrar el socket ya no marca sin señal en el acto (8/9, L7): las que se van las marca el barrido a los 30 s, y la suite lo espera |
| `informes` | Los CSV: que las horas salgan de los turnos y que un nombre con `;` o comillas no parta el archivo |
| `paneles` | Los paneles web, por lectura (revisión del 8/9, P1–P7): el service worker **manda a la red toda petición con `Authorization`** antes de mirar cualquier caché y no guarda la marca, los números, el perfil, las grabaciones, los informes ni el creador; **Babel con `integrity`** en Despacho y el creador, el mismo hash que la app del chofer; la sesión guardada vale para el gerente; al reconectar se vuelve a pedir la ruta que se miraba; **«Salir» llama a `POST /auth/logout`** en Despacho y en la web del chofer; «N EN RUTA» usa los conteos del servidor; y los tiempos relativos de la lista usan un reloj de 1 s, no `Date.now()` en el render. Y A7 (10/9): **la web del chofer ve el tráfico** (los campos en `gapMeta`, «en tráfico N min» en el rótulo, «mantené: no te apures hacia el embotellamiento» en la instrucción) **y las fotos** del hilo; y Despacho dice «GPS SIMULADO». Y la tanda 4 de la revisión del 10/9, **la web del chofer a la par de la app nativa**: el SOS va por `POST /sos` y sólo dice «enviada» si llegó (si no, «NO SALIÓ»), el GPS simulado sólo existe en demo (y va marcado), sin GPS se dice, las unidades sin señal van huecas, el `watchPosition` se para, el chofer web avisa tráfico, y «EN VIVO · N» es el `totalOnRoute` del servidor |
| `reloj` | El reloj atrasado del teléfono (`server/reloj.js`), puro: que un Android con la hora dos minutos atrás, mandando perfecto, **no quede gris el turno entero**; que se lo crea recién sostenido (tres envíos a lo largo de un minuto) y hasta diez minutos; y que **el vaciado de atraso de un corte no se confunda con él** (cada lote trae la posición de ahora, y el mínimo baja a cero) |
| `huecos` | **Señal y presencia** (`TRUCOS-2026-09-10.md`): cada corte de señal queda con dónde se cortó, dónde reapareció, cuánto duró y la velocidad implícita entre los dos puntos; **el corte de red se distingue del teléfono apagado** porque las posiciones del corte llegan después; un salto de más de 120 km/h queda anotado (uno por minuto); el ausente que se mueve por el trazado también; los minutos ausente se suman; todo sale en el resumen del gerente y en `senal.csv` / `anomalias.csv`; salir de ruta cierra el hueco y el que no vuelve en el plazo queda «no volvió» sin fin; y **nada de esto avisa en vivo** |
| `farsa` | El GPS que inventa sin el flag de Android (`server/farsa.js`), puro: que la combi de verdad —la que frena en cada esquina y zigzaguea 15 m, la que va pareja por la avenida con un GPS real, la que va a 60 clavados por la carretera con un GPS bueno— **nunca** quede sospechosa; que el simulador que dibuja la ruta a velocidad constante y clavado en la línea sí, con su motivo (`velocidad_constante`, `sin_ruido`), dicho **una vez** cuando se llena la ventana de doce; que la velocidad reportada que no cuadra con el desplazamiento también (`velocidad_incoherente`); que sin trazado las otras dos firmas sigan; y que doce posiciones de verdad limpien la sospecha |
| `trucos` | Los pasos 2 y 3 de `TRUCOS-2026-09-10.md` contra el servidor (y de la revisión del 10/9: la histéresis de la imprecisión, una anotación por minuto, la salida de ruta que se cierra igual con el GPS impreciso al declararse ausente, y el nombre del chofer en la unidad que sólo manda por HTTP): una posición con más de 100 m de error declarado **entra al mapa pero no juzga** —doce posiciones a 600 m del trazado con 400 m de error no son salida de ruta, y las mismas con 10 m sí; quieta con 500 m de error no es parada, con 10 m sí—, queda marcada y anotada una vez por episodio, y un APK viejo sin `precision` sigue como siempre; catorce posiciones con la firma de un simulador dejan a la unidad `gpsSospechoso` con motivo, en `anomalias` y en la auditoría, sin esconderla, y catorce de verdad la limpian; y el aviso de tráfico **sin parada medida** se cuenta aparte del que sí la tuvo (`paradas.medida`, `avisosSinParada` en el resumen y en los totales) |
| `horas` | **Las horas que se liquidan** (revisión del 10/9, tanda 1): el turno del chofer que trabaja por HTTP con la pantalla apagada se cierra al salir de ruta, al cerrar sesión y cuando deja de oírse —con su última señal, no con la hora del barrido, y si vuelve enseguida se retoma el mismo—; el cobrador tiene su turno y el latido del socket se lo mantiene vivo aunque no mande GPS; las horas por unidad son las del chofer solo; cambiar de combi con el turno anterior abierto cierra ése y abre otro; el turno que cruza el borde del período se recorta a los dos lados (4 h adentro de un turno de 12); y **el perfil, el gerente, la pestaña Turnos y el CSV dan el mismo número** para el mismo turno |
| `medicion` | **Lo que falseaba la medición** (revisión del 10/9, tanda 3): redibujar el trazado con el que se mide cierra el desvío abierto como «trazado» y limpia las marcas, igual que cambiar de variante; del vecino que quedó sin señal no se afirma «en tráfico»; el corte que empieza al cerrarse el WebSocket queda en `huecos`; la parada que se corta por el olvido se cierra con la hora de la última posición y no tres minutos después; el ausente que empezó antes del rango cuenta lo que cae adentro, y el que se murió ausente deja de contar (queda «olvido» en el registro); y las salidas silenciadas o cerradas por cambio de trazado se cuentan aparte, no como salidas |
| `numeros` | **Lo que el gerente y el creador no veían** (revisión del 10/9, tanda 5a): las vueltas se atribuyen a la PERSONA que tenía el turno de chofer cuando se cerraron (tres al de la mañana, dos al del relevo, el cobrador ninguna, y la que no cayó en ningún turno queda «sin atribuir», no se inventa); la flota entera aparece y la combi sin actividad se ve apagada con su última señal; las entradas tardías se cuentan; el resumen dice su alcance, acepta ruta y da 404 en la ajena; hay tendencia por día y CSV de paradas; el creador ve unidades-día del mes y la última señal de cada cooperativa; y el servidor avisa fuerte si arranca en UTC |
| `cadena` | **La cadena de brechas es por tramo** (decisión del 8/9): con dos de ida y una de vuelta, la que llega al final de la ida no tiene «adelante» a la que salió de vuelta, la recién salida no tiene «atrás» a la de vuelta, y la de vuelta está sola en su tramo; cuando la tercera cambia de tramo, recién ahí son vecinas; la vuelta entera se sigue guardando en `laps`; y una ruta sin trazado sigue siendo una sola fila |
| `csv` | Los CSV de los informes (`server/csv.js`): lo de siempre (comillas, punto y coma, saltos de línea) y que **una celda que empiece con `=`, `+`, `-` o `@` llegue a Excel como texto** — el alias lo escribe el chofer desde su app, y `=HYPERLINK(…)` o `=cmd\|…` se ejecutaba al abrir el informe |
| `parada` | La parada sostenida (`server/parada.js`), pura: que 50 s subiendo pasajeros en una esquina de Juliaca NO sea tráfico, que 3 min clavado SÍ, que el embotellamiento que arrastra a 1,8 km/h cuente igual y que a 3,6 km/h sostenidos no, que se salga con 100 m en el último minuto y un semáforo adentro del embotellamiento no apague y prenda el aviso, que el terminal y fuera de ruta no cuenten, que el cambio de vuelta no sea marcha atrás, y que `andando` diga cuándo apagar la palabra del chofer. Y del 10/9: **que las posiciones no tengan que caer justo en el borde de la ventana** — con pasos de 10,037 s (lo que manda un GPS de verdad) la parada se detecta igual a los 3 min; antes no se detectaba nunca, y las suites con pasos de 10 s exactos no lo veían. Y que **un hueco de datos no sea una parada**: dos muestras a diez minutos no declaran nada, y la parada en curso termina si se cortan los datos |
| `trafico` | Lo mismo contra el servidor de verdad, con los plazos en segundos: la unidad queda `parado` sola y con su hora, al de atrás le llega `aheadEnTrafico` en la brecha, el chofer confirma por `POST /trafico` y por WebSocket, las dos marcas se apagan solas al andar (con el minuto de gracia para el que marcó llegando), retirarla a mano existe, el terminal y la ausente no cuentan, cobrador 403 y sin booleano 400, `GET /admin/paradas` lista los episodios con dónde y cuánto, y al salir de ruta no queda ninguno abierto |
| `desvio` | Que el desvío se marque solo si se sostiene, y que se pueda silenciar. Y del 8/9: **sólo a las de la cadena** —yendo a 4 km o ausente por un repuesto no abren episodio— y **con la hora de la posición**: un atraso de 14 minutos vaciado de golpe es un episodio de 4 minutos, no de 0 ms |
| `turnos` | Que un corte de señal no parta el turno y que un reinicio no deje turnos abiertos para siempre (lo del cierre por «fuera», logout y olvido está en `horas`) |
| `privado` | El mensaje directo Despacho ↔ unidad: que lo vean los dos y nadie más |
| `seguridad` | Inyección por identificadores, fuerza bruta y cupo de mensajes por conexión |
| `empresas` | Que dos cooperativas no se vean **nada**: ni panel, ni mapa, ni chat, ni SOS. Incluido el privado de Despacho, que era la puerta que faltaba mirar: el código de vehículo es único en TODO el servidor y el envío solo comprobaba que existiera, así que acertar el código de una combi ajena alcanzaba para escribirle a su chofer — medido cruzando un mensaje de una empresa a otra, hoy cerrado y con la prueba puesta |
| `cobradores` | Que el chofer administre a los cobradores de SU combi —clave y baja—, y que ahí termine. Lo que se prueba no es que funcione sino dónde corta: **el alta por esta puerta no existe** (se le pega al endpoint, no se mira la pantalla: que un botón no esté no prueba nada), el cobrador del de al lado no se toca (404, y el error no confirma que exista), un cobrador no administra cobradores ni a su compañero, y el vecino no ve lo que no es suyo. Más lo que queda escrito: clave y baja auditadas —del cambio de clave, QUE la cambió y nunca cuál—, ninguna alta a nombre de un chofer, y la gerencia siguiendo viendo a todos |
| `variantes` | Rutas alternas: que cambiar el trazado descarte las vueltas en curso y no mezcle geometrías en el promedio |
| `brecha` | Que la brecha promedio por vuelta se guarde, sea creíble, y quede vacía cuando no hay con qué compararse |
| `creador` | Las cuatro barreras del nivel de arriba, incluido que sin `CREATOR_PASSWORD` responda 404 y no 403 |
| `gerencia` | La fusión de los dos paneles: el gerente entra a `/admin/*` y al tiempo real como un panel más, y puede lo que Despacho no — vehículos con placa, chofer-con-combi de una, datos de la empresa, logo. Despacho recibe 403 en todo eso (y el error dice quién sí), no le puede tocar la cuenta al gerente, y `/gerencia/*` sigue siendo solo del gerente. Y los CSV `senal` y `anomalias` (10/9) bajan para el gerente |
| `presencia` | Salir a ruta, ausente, fuera — y la CONFIRMACIÓN física. El caso tiene nombre: Ignacio marca "en ruta" desde su casa a las 5:30; Despacho lo ve "yendo", pero NO entra a la cadena de brechas hasta que el GPS lo ve **sobre el trazado** (el mismo umbral del desvío). AUSENTE (comer, un repuesto) lo saca de la cadena sin echarlo del mapa y los vecinos se recomponen; FUERA lo borra del mapa en el acto, sin esperar al olvido; y la presencia viaja pegada al `POST /gps`, así sobrevive a la pantalla apagada y a un reinicio del servidor. Sin declarar nada, todo sigue como siempre — la compatibilidad también se prueba. Y de la revisión del 8/9: **volver del almuerzo no es una entrada tardía** (se conserva cuándo se lo perdió), **el reinicio del servidor no acusa a nadie** (gracia de arranque, `ARRANQUE_GRACIA_MS`), y yendo desde su casa a 3 km del trazado **no es un desvío**. Y del 8/9 (A5): un `POST /gps` atrasado, de antes del «fuera», **no vuelve a poner la combi en el mapa** |
| `ausencia` | El vigía de la ausencia (`app/ausencia.js`): que el que arranca después de almorzar **vuelva a ruta solo** (dos posiciones seguidas lejos del ancla), que un salto de GPS de alguien sentado comiendo NO lo devuelva, que media hora de zigzag parado no dispare nada, y que pasadas 2 horas la ausencia se convierta en **fuera** — aunque se esté moviendo. Lógica pura: vive en la tarea de fondo y se prueba sin teléfono. Y del 8/9 (A9): **el ancla es donde quedó parado** —dos posiciones seguidas a menos de 40 m—, no la primera posición de la ausencia; declararse ausente en el terminal y manejar 800 m hasta el restaurante ya no lo devuelve a ruta al llegar |
| `senal` | Que una unidad que deja de reportar quede **sin señal** y no borrada: que la de atrás no salte a medirse contra la que sigue, que vuelva sola al reaparecer, que se olvide recién a los 3 min, y que ninguna brecha salga con los segundos en 60 |
| `gpshttp` | `POST /gps`: que la posición pueda entrar **sin WebSocket vivo**, que **el mando del GPS sea de una persona** (revisión del 10/9, C2: el que declara «ruta» lo toma, el relevado recibe `gpsRole: false` y no se le procesa nada, identificarse por WS lo devuelve), que `lat: 999` no entre (C1) y que un reloj adelantado reciba un 400 que dice cuánto (C5), que un atraso entero se mida con la hora de cada posición y no la de llegada, que el cobrador y los relojes mal puestos no pasen, que **la brecha vuelva en la respuesta** (es lo que mantiene viva la notificación con la pantalla apagada), que **vaciar atraso no sea estar muerto**: al que llega con posiciones viejas se lo oye — queda en gris, jamás borrado —, y que **lo que ya se sabía no se procese dos veces**: un lote repetido (un envío cortado con la pantalla apagada que llegó igual) o una posición más vieja que la conocida vuelve como «ya vista», no mueve la unidad ni pasa por la medición, y sólo cuenta como «al teléfono se lo oye». Y del 8/9: **el reloj atrasado del teléfono** (un minuto atrás, sostenido) saca a la unidad del gris y dice cuánto atrasa (`relojAtrasadoS`), mientras una sola posición vieja sigue siendo gris; y **cerrar el socket con el HTTP vivo no marca sin señal** —si de verdad se calla, la marca el barrido. Y del 8/9 (L6): **el relevo por HTTP no queda mudo con 409** —el que sube con la pantalla apagada toma el mando y al saliente, que dejó la app abierta, se le avisa que su GPS ya no se usa. Y de `TRUCOS-2026-09-10.md` (T2): **el GPS simulado se dice, no se esconde** — una posición con `simulado: true` entra igual, la unidad queda marcada, la auditoría lo anota una vez por episodio, y con el GPS real la marca se va |
| `hud` | Qué se le muestra al chofer en la app nativa: los tres estados de un lado, cuál es el dígito grande, los colores y el texto de la notificación — y el vecino en tráfico: el rótulo con los minutos, la instrucción que pasa a «mantené» y nunca «apurá» hacia el embotellamiento, y la notificación viva diciendo lo mismo. Sin servidor — es lógica pura |
| `chat` | El chat de la app nativa: que un privado no aparezca en el grupo, que Despacho se lea como Despacho, que el hilo no repita al reconectar y que lo propio no cuente como sin leer |
| `cola` | Las posiciones guardadas cuando no hay datos: orden, tope, y que un corte a la mitad de la descarga no las pierda |
| `envio` | El vigía del envío (`app/envio.js`): qué pasa con un `POST /gps` que no vuelve. Que un envío reciente haga esperar a la tanda nueva, que uno **colgado se corte desde la propia tarea del GPS** —el único reloj que late con la pantalla apagada: en Android los timers de JavaScript no corren con la actividad pausada, y el `setTimeout` de 15 s se quedaba esperando junto con el fetch—, que sus posiciones vuelvan a la cola una sola vez, que el envío cortado al morir tarde no pise al que arrancó después, que la cola se mezcle por hora y tire las viejas y nunca las nuevas, que lo que la tarea ya entregó no se entregue dos veces (expo-task-manager relanza el job pendiente con todo lo acumulado), y que el módulo no use ningún timer. Lógica pura, con el reloj inyectado |
| `pedido` | El pedido HTTP de la tarea de fondo (`app/pedido.js`): **sin `fetch` y sin timers**. En React Native `fetch` resuelve con `setTimeout(0)`, y en bridgeless ningún timer corre con la actividad pausada — la app mandaba y nunca se enteraba de la respuesta (1 h 47 min de «150 posiciones, 150 ya vistas»). Va por `XMLHttpRequest`, cuyos eventos salen directo del evento nativo, con el timeout nativo de OkHttp. Se prueba con un XHR de mentira resolviendo sólo microtareas: la respuesta llega, el 4xx trae su cuerpo, sin red y timeout tienen nombre, el abort de afuera corta en el acto y lo que llegue después se ignora; y falla si `gps/servicio.js` vuelve a llamar `fetch` |
| `foto` | Las fotos contra el servidor de verdad: que una de más no pase, que el tope del cliente y el del servidor sean **el mismo número** (el servidor descarta en silencio: si se separan, el chofer ve su foto salir y nunca llega), que una privada no se filtre al grupo, y que una ráfaga de fotos no le borre el audio a las notas de voz. Y del 8/9 (P8): el servidor mira el **formato**, no sólo el prefijo — un SVG por un cliente crudo no entra, un WebP sí |
| `margenes` | Dónde terminan las barras de Android en cada configuración —botones, gestos, muesca, horizontal, y un Android viejo que reporta 0—. Existe por un bug medido: el botón de CHAT quedaba **debajo** de los botones del sistema |
| `gestos` | Pasar de pantalla deslizando **sin** robarle el gesto al SOS ni al scroll del chat. Un falso SOS moviliza gente; un scroll que cambia de pantalla hace el chat inusable |
| `imagen` | Las cuentas de la foto: a qué medida se achica según sea vertical u horizontal, que una chica no se AGRANDE, y que el peso no se confunda con el largo del base64 (infla 4/3, y confundirlos rechaza fotos que entraban) |
| `tema` | Los colores de día y de noche: que la regla horaria sea la de Juliaca, que la de noche sea más oscura y **menos azul** (el azul es lo que arruina la visión nocturna del chofer), que verde/ámbar/rojo sigan siendo reconocibles, y que ninguna paleta tenga huecos |
| `contraste` | Las paletas de las tres pantallas **web** (`tema` cubre la nativa): que cada par tinta/fondo que el código realmente compone mida AA 4.5:1 —o 3:1 solo en el número gigante de brecha—, que el tema `sun` lea **mejor** que `day` en todos los pares, que `day` siga siendo el de fábrica, que `#FF2D55` aparezca solo como rojo de emergencia de `night`, y que el `:root` del creador no se separe en silencio de la paleta `day` de Despacho, de la que es copia. Lo que pasa su piso pero no llega a AA se lista en cada corrida, informativo |
| `mapa` | Qué se dibuja en el mapa del chofer: que la unidad sin señal siga apareciendo pero apagada, que una sin coordenadas no termine en el Golfo de Guinea, y que un apellido con comilla o con `<` no rompa la página del WebView — los nombres los tipea Despacho a mano |
| `marca` | La identidad de cada cooperativa: qué logo se acepta (los SVG NO — son documentos con scripts adentro y esto se pinta en el panel y en el WebView del chofer), qué se muestra cuando no hay logo, y sobre todo que **una cooperativa no le pueda pisar el logo a la de al lado** mandando el companyId ajeno. Ahí adentro está también la prueba de que un trazado real de 2000+2000 puntos entra por el cable: el tope de cuerpo de express venía más chico que lo que el servidor decía aceptar |
| `trazador` | La lógica del trazador del panel del creador (`server/trazador.js`), sin mapa ni navegador: que un clic **sobre la línea** inserte en el segmento correcto y uno lejos agregue al final, que borrar entre dos puntos respete los elegidos y no le importe el orden, que **deshacer** devuelva el estado de antes aunque el presente se haya seguido mutando, que simplificar borre el zigzag del GPS pero no las esquinas, y que un GPX o GeoJSON se lea con lat y lng en su lugar. El mismo archivo que corre en el navegador, probado con `require()` |
| `respaldo` | El respaldo de la base, juzgado por lo único que importa: **la restauración**. Respalda una base real en caliente, la abre de vuelta y lee las filas; rechaza la basura y la base equivocada; rota borrando los más viejos; y por el panel del creador crea, lista y **descarga** — y lo descargado se abre y tiene el DESPACHO adentro. Un `../` en el nombre no es un nombre |
| `teclado` | Cuánto levantar la pantalla cuando sale el teclado de Android. Existe porque el campo de escribir quedaba DEBAJO del teclado: `KeyboardAvoidingView` sin `behavior` no hace nada en Android, y lo que sí resolvía el sistema —achicar la ventana— no ocurre con edge-to-edge. La cuenta fina es no contar dos veces la barra de navegación, que la pantalla ya reservó |
| `nativas` | Las versiones de los módulos nativos de la app: que ningún `expo-*` venga de otro SDK, que no haya un módulo duplicado, que el lockfile no quede viejo, y que no se use una API que Expo dejó como stub que revienta. Lee el lockfile: sin teléfono, sin red, un segundo. Existe porque un `expo-asset` del SDK 57 al lado de un `expo-modules-core` del 54 dejó la app sin abrir, y eso solo se veía con el APK ya instalado, veinte minutos de build después. Y la **tanda 4 de la revisión del 8/9, por lectura**: las cuatro fases del SOS, `limpiarSesion()` llamada desde «Salir» y `auth_error`, el apagado por 401/403/409 sostenidos, la puerta del cobrador, un solo tope de foto, `allowBackup=false`, el reintento del socket protegido, la presentación con tope, y `versionCode` subido. Y del 10/9: **una sola pantalla de arranque** (la nativa es sólo el fondo, sin ícono), `versionCode` 4, y la tarea manda `simulado: true` cuando Android marca la posición como falsa |
| `vendor` | Que **ninguna pantalla dependa de un CDN para poder dibujar el mapa**. No es una precaución teórica: unpkg no le entregó `leaflet.js` al navegador del creador y elegir una ruta dejaba la página en blanco, sin un solo error a la vista — `L` no existía y el script se cortaba en la primera línea. Verifica que el servidor entregue Leaflet de verdad (y no un 404 con forma de página), que los tres HTML lo pidan a su propio origen, que el WebView del APK lo lleve adentro del bundle, y que **la copia del APK y la del servidor sigan siendo la misma**: si alguien sube la versión en `server/vendor/` y se olvida de correr `herramientas/vendor-leaflet.js`, la app se queda con la vieja y nadie se entera hasta ver algo raro en el teléfono |
| `retencion` | Cuánto historial se guarda, y que **el tamaño de la flota no lo decida**. Los topes eran de filas y globales —2000 vueltas, 1000 mensajes—: con seis combis son meses, con 2000 unidades son tres horas de vueltas, y como el SOS vive en la misma tabla que el chat, una tarde de conversación activa borraba las emergencias del mes y el informe salía vacío sin que nada lo dijera. Siembra filas con fecha puesta a mano —para no esperar cuatro meses—, reinicia el servidor y mira qué sobrevivió: la vuelta de hace 119 días sí y la de 200 no, la charla de hace dos meses no y **el SOS de ese mismo día sí** | 
| `cliente` | El cliente del protocolo que va a usar la app nativa (`app/protocolo/`): el rol de GPS cuando hay relevo, las brechas que respetan el null, el freno de cadencia y el privado que no se filtra. Y del 8/9: **`mandarSos` resuelve recién con el eco** (por el socket) o **por HTTP sin socket**, emitiendo el eco él mismo; sin sesión dice que no salió en vez de mentir; `marcarPresencia('fuera')` se puede esperar y contesta por HTTP; **`cerrarSesion()` revoca el token en el servidor** (después, 401) |
| `tiles` | De dónde vienen las tiles del mapa — y de dónde NO pueden venir. CARTO y el CDN de OSM quedaron fuera de licencia para uso comercial: la suite busca los hosts prohibidos como host completo (un comentario también falla) y exige que las URLs de tiles sean del propio origen o del proveedor con clave, con la clave fuera del código |
| `renovacion` | **Renovar el mapa propio en un servidor que ya lo tiene.** Era un procedimiento manual —vaciar la carpeta del volumen y redesplegar— porque los archivos se llamaban siempre igual y el servidor solo bajaba lo que le faltaba: como no le faltaba nada, el mapa nuevo no llegaba nunca. Ahora la versión va en el nombre, y la suite lo corre entero contra el servidor real con un release de mentira: arranque en frío, mapa renovado que **baja solo y borra el que reemplazó** (si no, cada renovación deja tirada una copia entera del mapa viejo en un volumen que se paga por GB), el estilo que no cambió que no se vuelve a bajar, y un reinicio sin release nuevo que no mueve un byte. Más las dos URLs de tile (la versionada inmutable y la de siempre, que es la que piden los APK ya instalados) y la punta que no se ve corriendo: que el extractor ponga la versión en el nombre y las dos pantallas la pongan en la URL |
| `cascada` | La cascada de tiles vista desde el navegador del chofer: en los zooms cubiertos, las tiles salen del mapa propio y el proveedor no recibe **ni un pedido**; la tile que el propio no tiene —y solo esa— cae al proveedor; los contadores (`window.TILES_STATS`) cuentan la verdad, porque son la evidencia de que el proveedor es la excepción; y cada tile propia se pide **con la versión del mapa adentro de la URL**, que es lo que hace que una renovación le llegue al celular en vez de quedarse con la copia guardada |
| `mapa-shot` | Banco visual del MAPA en las cuatro pantallas, que **falla si el lienzo sale vacío**: la vara es tiles efectivamente cargadas (no pedidas) y algo dibujado encima. Un mapa en blanco no avisa — se descubre arriba de la combi |
| `compresion` | Las dos palancas de ahorro con código (`COSTOS.md` §5). El índice de vueltas existe y el plan de consulta lo usa; y la compresión del WebSocket se mide en el cable: dos espectadores reciben los MISMOS estados y el que negoció permessage-deflate tiene que recibir menos de la mitad de los bytes (medido: −90 %). El que no ofrece la extensión —la app nativa— sigue funcionando sin comprimir, y un chat mandado comprimido llega intacto al que no comprime |
| `sos` | El tipo de emergencia y el orden de las cosas: **deslizar manda la alerta YA**, sin preguntar nada; el tipo ("falla mecánica", "accidente", "policía") va después, con la emergencia ya sonando en Despacho. Los bordes: solo quien disparó puede calificar la suya, solo mientras la ventana está abierta, un tipo inventado no entra, y el historial y el CSV cuentan lo mismo que se vio en vivo — sin elegir queda "SOS" a secas, que es como nace cada uno. Y del 8/9 (A1): **`POST /sos`** dispara lo mismo que el socket y contesta con la alerta y su id (se puede tipificar después con ese id); sin sesión 401, Despacho 403, coordenadas inválidas salen sin posición, más de cinco por minuto 429 |
| `perfil` | El perfil del conductor: **lo SUYO y nada más** (no hay parámetro de unidad: todo sale de la sesión). El espejo usa el mismo criterio que el gerente —vueltas del vehículo, horas de la persona, cumplimiento contra la vara que regía—, el alias que se edita llega **en vivo** al mapa de Despacho sin que nadie vuelva a entrar, y la contraseña se cambia con la actual en la mano, auditando QUE la cambió y nunca cuál es |
| `puertas` | Quién entra, con qué, y hasta dónde llega adentro. Salieron de una revisión de seguridad del sistema entero, y ninguna se veía usando la app: el **bootstrap de `DESPACHO`** ya no es una puerta permanente —funciona en un sistema vacío, y con cualquier cuenta de administración existente contesta 404 sin crear nada—; el **bloqueo por origen** deja de leer el primer elemento de `X-Forwarded-For` (el que escribe el cliente) y cuenta desde la derecha, con y sin proxy; **cerrar sesión existe** y mata el token en el SERVIDOR, con variante "todas"; **cambiar la clave propia echa al que copió el token** sin echar al dueño de su teléfono; el **alcance por ruta** vale también para grabaciones y logo; **lo que manda el teléfono se valida** —una coordenada que no es número no entra al mapa, un progreso de 999 no le arruina la brecha al de adelante, un SOS con basura no tumba el proceso y queda con una hora que sí aparece en el informe—; y una **sesión revocada corta también el WebSocket**, que antes vivía para siempre de la confianza del saludo . Y las **llaves de arranque** (10/8): en producción el servidor no arranca si `DISPATCH_PASSWORD` falta, es corta o está quemada, si `CREATOR_PASSWORD` está quemada, o si las dos son la misma — incluida la parte que más importa, que **frene ANTES de grabar la cuenta**, porque puesto más abajo el arranque alcanzaba a instalar justo la clave que estaba rechazando. También que la huella del dueño en `CLAVES_QUEMADAS` frene, que la clave rechazada **no se imprima nunca**, y que en `MODO=demo` no se revise nada. Y **un socket roto no tumba el servidor** (8/9, de `REVISION-2026-09-08.md`): un frame WebSocket inválido o un mensaje `null` —sin token, antes del `identify`— dejaban el proceso en el piso; ahora ese socket se cierra y el resto sigue; un frame de 3 MB cierra sólo esa conexión (1009); un cuerpo JSON malformado contesta 400 en JSON, sin stack ni `X-Powered-By`, y uno de más de 1 MB, 413. Y de la revisión del 8/9 (tanda 5, 9/9): **un socket que no se identifica se cierra a los 15 s** (código 4008) y un latido termina al que no contesta el ping (S5); **el login no distingue «no existe» de «clave mala»** —mismo 401, mismo texto— (S6); y toda respuesta trae **las cabeceras de seguridad** (CSP a la medida de los paneles, nosniff, X-Frame-Options DENY, Referrer-Policy, HSTS) con `/ping` que ya no cuenta unidades ni conexiones (S7, S9) |
| `metidos` | El que **se mete a la ruta empezada** y el que hace **media vuelta**, sobre el circuito duro (ida y vuelta por la misma calle). Del primero: se lo confirma en ruta igual, pero marcado (`entradaTardia` con el punto por el que entró) y auditado; su primera vuelta se guarda con `parcial = 1` y queda fuera de todo promedio —resumen, cuadro por unidad, objetivo— **sin desaparecer de la lista**, y la siguiente, ya desde el inicio, sí cuenta. Del segundo: hace la ida, declara "fuera" arriba, y la ida **queda guardada** en `legs` aunque no haya cerrado ninguna vuelta; Despacho la ve con 1 ida y 0 retornos, y el perfil del chofer dice lo mismo. Más el informe `tramos.csv` y la columna que dice cuál vuelta no es entera. Y el caso que puede arruinar la detección entera, con su propio servidor y el olvido en 2 s: **una zona muerta larga no convierte al chofer en uno que se metió** — sale del inicio, se calla más de lo que aguanta el olvido, reaparece a mitad de ruta, y vuelve a la cadena **sin** quedar marcado ni acusado en la auditoría |
| `periodo` | Que el cuadro por unidad de Despacho muestre **un período y diga cuál**. La mitad de rendimiento es lo de menos: lo que se defiende es que el número y el rótulo digan lo mismo, porque una pantalla acotada que sigue diciendo "acumulado" es peor que una sin acotar — el despachador lee un total y está viendo una semana. Se prueba que el default sean **7 días** y no todo el historial; que el período pedido se aplique **a los datos** y no sólo se acepte (una vuelta de hace 20 días no aparece a 7 días y sí a 30, una de hace 200 sólo con `?todo=1`); que `legs` se corte con el mismo filo, para que una unidad no entre a una tabla de 7 días arrastrada por sus idas viejas; y que un pedido absurdo (`?dias=9999`, `0`, `abc`) se recorte y **el servidor lo diga** en `periodo`, que es el campo con el que la pantalla rotula |
| `panel-periodo` | La otra mitad de `periodo`, mirada en un navegador de verdad: **que la pantalla no afirme un período que no es el que muestra**. El encabezado nombra el período que está viendo, cambiarlo cambia el rótulo y no sólo los datos, la palabra **"acumulado" no aparece en ningún lado** y la columna dejó de llamarse "Total" —un total de siete días no es un total—. Va con navegador y no con una expresión regular sobre el HTML porque el rótulo se arma en ejecución con lo que contestó el servidor: leer el archivo probaría la plantilla, no lo que se ve. De paso valida que el JSX compile, que es algo que ninguna otra suite mira |
| `avisos` | El canal del nivel de arriba hacia una cooperativa. El aviso lo manda SOLO el creador (un token de Despacho en esa puerta es 401), aparece como banner en el panel de Despacho de ESA empresa y de ninguna otra (el borde se prueba con una segunda cooperativa real), un chofer no lee avisos, y **no desaparece hasta que alguien lo marca visto** — del visto queda quién y cuándo, el creador lo ve, y la auditoría de la cooperativa también. Los vistos de más de un año se van solos; **los pendientes no caducan nunca**: un aviso de deuda sin ver no puede desaparecer callado |
| `poda` | El techo de filas del historial recorta **exactamente** lo mismo después de reescribirlo. `podarHistorico()` cortaba con `id NOT IN (SELECT id … LIMIT N)`, que recorre la tabla entera aunque no haya nada que borrar —el 99 % de un arranque de 38 s a 5000 unidades—; ahora corta por rango. Que sea equivalente es lo que hay que **demostrar y no suponer**: esto borra historial de verdad, y un borrado de más no se nota hasta que alguien pide un informe viejo y no está. Corre las dos formas sobre copias idénticas y compara **los ids que sobreviven, uno por uno** —no cuenta filas—, con ids salteados (después de podar por fecha quedan huecos), techo justo, techo 0 y tabla vacía. Cambiar `<=` por `<` hace fallar 5 de los 10 casos. Además revisa el `server/index.js` real para que el patrón lento no vuelva a entrar por descuido. Y del 8/9 (L8): **el techo de filas de `messages` no puede borrar un SOS** —cuenta y borra sólo lo que no es SOS; el SOS queda protegido por su retención de 365 días |
| `emision` | El índice de unidades por ruta nunca se separa del mapa plano. `buildState` dejó de barrer la flota entera para armar el estado de una ruta —era cuadrático (`herramientas/emision.js`)— y ahora lee un índice `routeId → Set`; un índice que mienta saca unidades fantasma en el mapa de Despacho, peor que el barrido lento. No lee el código: fuzzea el módulo real (`server/indice-unidades.js`) con 5000 operaciones al azar —alta, baja y sobre todo **cambio de ruta**, el caso que obliga a centralizar la mutación— y tras CADA una reconstruye el índice desde el mapa y exige que coincidan. Incluye un caso que rompe el índice a mano para probar que el verificador tiene dientes |
| `grabador` | El grabador de recorridos, en sus dos mitades. La pura: **un punto cada 30 m RECORRIDOS** (el semáforo no genera un nudo de puntos), el paso medido desde el último guardado, retomar tras un proceso muerto, y el GeoJSON con las coordenadas [lng, lat] que el formato exige. La del servidor: la grabación sube por POST /grabacion (el largo lo calcula el servidor, no se le cree al teléfono), el panel la lista y la baja como GeoJSON —la puerta que el trazador ya importa—, un chofer no lista las ajenas, y las viejas se podan solas al tope de 25 por empresa. Y el **pedido de Despacho** (4.5): viaja en la respuesta del POST /gps y sólo ahí, la app confirma con `grabando: true` y el panel pasa de *esperando* a *grabando*, subir la grabación —o dejar de grabar— lo limpia, cancelar lo apaga antes de que la app lo levante, repetirlo sobre una grabación en curso no manda otro "arrancá", el borde de empresa lo trata como inexistente (404) y a las 12 horas se vence solo |

## Sintaxis de las pantallas

Los paneles no tienen paso de compilación: Babel corre en el navegador, así que
un error de sintaxis solo aparece al abrir la página. Esto lo adelanta:

```bash
npm run sintaxis
```

Compila el bloque `<script type="text/babel">` de cada `.html` con el mismo
Babel que usa el navegador y dice en qué línea del archivo está el error.

Corre además `chk-rn.js`, que hace lo mismo con los archivos de la app nativa
(`app/`). Hace falta porque usan JSX e `import` —`node -c` no los parsea— y
porque las suites solo cargan los módulos puros: un error de tipeo en
`App.js` o en el servicio de GPS no aparecería hasta tener el teléfono.

## Bancos visuales

No son pruebas: levantan el servidor con datos sembrados, abren las pantallas
en Chromium y sacan capturas en `pruebas/shots/`. Sirven para mirar el
resultado y —esto es lo que más valió— para enterarse de que la página reventó,
porque listan cualquier error de JavaScript al final.

```bash
node rediseno.js        # Despacho, Gestión y el trazador
node gerencia-shot.js   # los números del gerente, con tres semanas de historial
                        # (entra por despacho.html: gerencia se fusionó con Despacho)
node creador-ui-run.js  # el panel del creador (esta sí verifica, no solo mira)
node chofer-shot.js     # la app del chofer (esta también verifica; desde el 10/9 corre en la regresión)
```

`chofer-shot.js` es la que más tardó en existir y la que más encontró: entra
como chofer con dos unidades en ruta, le mueve el GPS al navegador y revisa
que lo que se ve sea del servidor. Lo que atrapó la primera vez fue una
familia entera de datos de maqueta que se colaban como si fueran en vivo —una
unidad de atrás que no existía, una velocidad de 28 km/h con la combi parada,
un tramo de dos ciudades de la costa— porque el cliente tapaba con `||` los
`null` que el servidor manda bien.

Necesitan Chromium. En este entorno está en `/opt/pw-browsers/chromium`; en otro,
cambiar el `executablePath` o instalar el de Playwright.

`cdn.js` guarda en disco lo que se baja de CDN (React, Babel, Leaflet, fuentes,
tiles) para que una caída de red no deje la página en blanco a mitad de una
corrida. Baja **de a una y con reintento**: el proxy del sandbox contesta 503
cuando le entran varias juntas, y el navegador pide React, React-DOM, Babel,
Leaflet y las fuentes en paralelo. Sin eso el caché nunca llegaba a poblarse
en la primera corrida y los bancos morían con la página en blanco.

## Lo que estas pruebas NO cubren

Y conviene tenerlo escrito, porque verde acá no significa que funcione en la
calle:

- **Nada de lo que pasa en un celular de verdad**: batería con el GPS prendido
  a 3800 m, la app en segundo plano, señal que va y viene, el chofer mirando o
  no la pantalla. Eso solo lo contesta una semana en ruta.
- **La precisión del GPS real.** Las suites mandan coordenadas perfectas; en la
  calle hay rebote entre edificios y derivas de decenas de metros.
- **Carga.** Todo corre con un puñado de unidades. Los números de 20+ rutas
  están estimados en `ESCALABILIDAD.md`, no medidos.
