{
  "name": "coop-r14",
  "private": true,
  "description": "Control de flota para cooperativas de transporte: app del chofer, panel de Despacho y servidor de tiempo real",
  "scripts": {
    "postinstall": "cd server && npm install --omit=dev",
    "start": "node server/index.js",
    "test": "cd pruebas && npm test"
  },
  "engines": {
    "node": "22.x"
  }
}
